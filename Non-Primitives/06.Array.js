// Array Static methods

// Array.from() Creates a new Array instance from an iterable or array-like object

let welcome = Array.from("Welcome");
console.log(welcome);

let numbers = Array.from([1, 2, 3, 4]);

console.log(numbers);
